export const findIndex = (array, value) => {
  return array.indexOf(value);
  // if (value === array[0]) {
  //   return 0;
  // } else if (value === array[4]) {
  //   return 4;
  // } else if (value === array[2]) {
  //   return 2;
  // } else if (!array.includes.value) {
  //   return -1;
  // }
};

// use index of or find index method
